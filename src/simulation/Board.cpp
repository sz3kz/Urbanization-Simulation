#include "Board.hpp"

template<typename T>
unsigned int Board<T>::calculateCellIndex(Coordinates const& coordinates)
{
}
bool Board::checkCellExists(Coordinates const& coordinates);
bool checkCellEmpty(Coordinates const& coordinates);